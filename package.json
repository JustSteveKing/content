{
  "name": "content",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "verify-schema": "bun scripts/verify-schema.ts",
    "stats": "bun scripts/stats.ts",
    "sync-youtube": "bun scripts/sync-youtube.ts",
    "sync-packages": "bun scripts/sync-packages.ts"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1",
    "gray-matter": "^4.0.3"
  }
}
